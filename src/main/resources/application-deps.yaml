spring.application.name: uservices/dojrzewatr

spring.cloud.zookeeper.discovery.root: /pl
spring.cloud.zookeeper:
  # prefix is not working ATM - it's not combining the uservices/${spring.application.name}
  prefix: uservices/
  dependencies:
    prezentatr:
      path: prezentatr
    butelktar:
      path: butelkatr