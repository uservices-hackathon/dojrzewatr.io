spring.application.name: uservices/dojrzewatr

logging:
  pattern:
    level: '%clr([trace=%X{X-Trace-Id:-},span=%X{X-Span-Id:-}]){yellow} %5p'

server:
  port: 9093

spring.cloud.zookeeper.discovery.root: /pl
spring.cloud.zookeeper:
  # prefix is not working ATM - it's not combining the uservices/dojrzewatr
  prefix: uservices/
  dependencies:
    prezentatr:
      path: prezentatr
    butelkatr:
      path: butelkatr